============
Installation
============

At the command line::

    $ easy_install musical-games

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv musical-games
    $ pip install musical-games
